services:
  nel.re:
    container_name: neo4j
    image: 'neo4j:community'
    restart: unless-stopped
    ports:
      - '7687:7687'
      - '7474:7474'
    volumes:
      - ./data:/data

    networks:
      - proxy

networks:
  proxy:
    driver: bridge
